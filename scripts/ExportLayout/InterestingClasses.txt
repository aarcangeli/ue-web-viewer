# This file lists the classes for which we want to extract layout information.
# When a class is selected, its full class hierarchy is also exported, along with all types referenced in
# its properties.
#
# Syntax:
#   # The class "UStaticMesh" (without the initial "U") is selected.
#   StaticMesh
#   # All classes in the "CoreUObject" module are selected.
#   CoreUObject.*
#   # The class "UUnrealEditorSubsystem" is not selected, even if it is referenced.
#   !UnrealEditorSubsystem
#
# Operators:
#   * - Matches any sequence of characters.
#   ? - Matches any single character.

StaticMesh
